name: flutter_keyboard_aware_dialog_example
description: An example project for keyboard aware dialog

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_keyboard_aware_dialog:
    path: ../

flutter:
  uses-material-design: true
